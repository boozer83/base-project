<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.example.app.domain.entity.User">
        <id property="id" column="id"/>
        <result property="googleId" column="google_id"/>
        <result property="email" column="email"/>
        <result property="name" column="name"/>
        <result property="picture" column="picture"/>
        <result property="role" column="role"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByGoogleId" resultMap="userResultMap">
        SELECT id, google_id, email, name, picture, role, created_at, updated_at
        FROM users
        WHERE google_id = #{googleId}
    </select>

    <select id="findByEmail" resultMap="userResultMap">
        SELECT id, google_id, email, name, picture, role, created_at, updated_at
        FROM users
        WHERE email = #{email}
    </select>

    <select id="findById" resultMap="userResultMap">
        SELECT id, google_id, email, name, picture, role, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (google_id, email, name, picture, role)
        VALUES (#{googleId}, #{email}, #{name}, #{picture}, #{role})
    </insert>

    <update id="update">
        UPDATE users
        SET email      = #{email},
            name       = #{name},
            picture    = #{picture},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>
