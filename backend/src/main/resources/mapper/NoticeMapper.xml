<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.NoticeMapper">

    <resultMap id="noticeResultMap" type="com.example.app.domain.entity.Notice">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="authorId" column="author_id"/>
        <result property="authorName" column="author_name"/>
        <result property="isPinned" column="is_pinned"/>
        <result property="viewCount" column="view_count"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="noticeResultMap">
        SELECT id, title, content, author_id, author_name, is_pinned, view_count, created_at, updated_at
        FROM notices
        ORDER BY is_pinned DESC,
        <choose>
            <when test="sortBy == 'viewCount'">view_count</when>
            <when test="sortBy == 'title'">title</when>
            <otherwise>created_at</otherwise>
        </choose>
        <choose>
            <when test="sortOrder == 'asc'">ASC</when>
            <otherwise>DESC</otherwise>
        </choose>
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM notices
    </select>

    <select id="findById" resultMap="noticeResultMap">
        SELECT id, title, content, author_id, author_name, is_pinned, view_count, created_at, updated_at
        FROM notices
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notices (title, content, author_id, author_name, is_pinned)
        VALUES (#{title}, #{content}, #{authorId}, #{authorName}, #{isPinned})
    </insert>

    <update id="update">
        UPDATE notices
        SET title      = #{title},
            content    = #{content},
            is_pinned  = #{isPinned},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM notices WHERE id = #{id}
    </delete>

    <update id="incrementViewCount">
        UPDATE notices SET view_count = view_count + 1 WHERE id = #{id}
    </update>

</mapper>
